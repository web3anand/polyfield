{
  "version": 2,
  "buildCommand": "npm run build:client",
  "outputDirectory": "public",
  "functions": {
    "api/*.ts": {
      "runtime": "@vercel/node"
    }
  },
  "rewrites": [
    {
      "source": "/api/(.*)",
      "destination": "/api/$1"
    }
  ]
}
